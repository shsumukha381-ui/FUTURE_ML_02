# Dependencies for Vercel deployment (serverless inference only).
# For training pipeline deps, see requirements-training.txt.
fastapi>=0.100.0
numpy>=1.24.0
scikit-learn>=1.5.0
pydantic>=2.0.0
python-multipart>=0.0.6
joblib>=1.3.0
