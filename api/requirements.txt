# Vercel serverless function dependencies (inference only)
# Kept minimal to stay well within Vercel's 250MB deployment limit.
fastapi>=0.100.0
numpy>=1.24.0
scikit-learn>=1.5.0
pydantic>=2.0.0
python-multipart>=0.0.6
joblib>=1.3.0
